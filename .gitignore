# Composer
/vendor/
composer.lock

# Restrukturyzacja "workspace/" (wip/workspace-restructure) - ma zyc na swoim branchu,
# nie mieszac sie z innymi (np. fix/*). Tu byla /vendor/ zakotwiczone tylko do roota,
# wiec nie lapalo zagniezdzonego workspace/vendor/ - stad przypadkowe git add -A na
# 5000+ plikow (w tym realna baza workspace/docs/devbrain.db i prywatne zdjecia
# z workspace/tests/img-to-test/). 2026-07-22.
/workspace/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# Storage
storage/
tmp/

# Tests / cache narzedzi jakosci
.phpunit.result.cache
/.phpunit.cache/
.php-cs-fixer.cache
coverage/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# Local OCR test fixtures (may contain personal/sensitive images) - never commit
/test-fixtures/
/tests/img-to-test/

# Claude Code local tool settings (machine-specific, not project config)
.claude/

# Lokalny scratch + WSZYSTKIE sekrety (klucze Google SA) - nigdy do repo
/.local/
mariusz-nauka-*.json
/docs/devbrain.db
/.local/
/img.png
